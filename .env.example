# 이 파일을 복사해 .env로 만들고 실제 값을 채운다. .env는 git에 올리지 않는다.

# 서비스 계정 JSON 키 파일 경로 (credentials/ 폴더 안에 둔다)
GOOGLE_SHEETS_CREDENTIALS_PATH=credentials/service_account.json

# 스프레드시트 URL의 /d/ 와 /edit 사이 값
GOOGLE_SHEETS_ID=your-google-sheet-id
