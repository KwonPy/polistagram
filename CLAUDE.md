# Polistagram

## 1. 프로젝트 개요

Polistagram은 사용자의 개인 상황에 맞는 금융정책을 선별해,
카드뉴스 형태의 개인화된 피드로 보여주는 서비스이다.
정책의 핵심 내용과 관련성, 필요한 행동까지 카드 한 장 단위로 안내한다.

**알림 서비스는 만들지 않는다.** 이메일은 회원가입/로그인 없이
**사용자를 구분하기 위한 식별자**로만 쓰고, 알림 발송 용도로는 쓰지 않는다.
서비스의 핵심 경험은 어디까지나 **개인화된 카드 피드**다.

### 해결하려는 문제

* 금융정책은 지속적으로 발표·변경되지만 내용이 복잡하고 이해하기 어렵다.
* 사용자는 "이 정책이 나와 관련 있는가?"를 판단하기 어렵다.
* 관련 정책을 찾아도 핵심 내용과 실제 신청/이용 행동을 파악하기 어렵다.

### 핵심 가치

**정책 발견 → 쉬운 설명 → 개인 관련성 → 행동 안내**

이 흐름을 **카드뉴스 형태의 개인화 피드**로 전달한다.

단순 정책 나열, 검색, 요약 서비스가 목표가 아니다.

---

## 2. 프로젝트 기간 및 목표

* 전체 기간: 약 5주
* 실제 구현 가능 기간: **약 4주**
* 목적: 사업화보다 **AI FDE 역량 학습 및 포트폴리오 구축**

### 학습 목표

* 외부 데이터 수집 및 자동화
* 데이터 정제 및 구조화
* LLM 활용
* 데이터 모델링
* 개인화 로직
* 정책 변경 감지
* API 설계

---

## 3. 데이터 범위

### 정책 범위

**금융정책으로 한정**

모든 정부정책을 다루지 않는다.

### 데이터 출처

**금융위원회 공식 RSS**

URL:
http://www.fsc.go.kr/about/fsc_bbs_rss/?fid=0111

### 수집 원칙

RSS 기반으로 신규 정책/보도자료를 감지하고,
필요한 경우 원문을 가져와 AI 분석에 사용한다.

### 저장소

**Google Sheets**

별도의 PostgreSQL 등 관계형 DB는 사용하지 않는다.

---

## 4. 핵심 기능

### ① 정책 수집

금융위원회 RSS를 주기적으로 확인하고 신규 데이터를 수집한다.

### ② 정책 구조화

정책 원문을 Gemini로 분석하여 핵심 정보를 구조화한다.

예상 정보:

* 정책명
* 정책 분야
* 대상
* 주요 조건
* 혜택/지원내용
* 신청/이용 기간
* 시행일
* 신청방법
* 행동 단계
* 원문 URL

### ③ 개인화

사용자 상황을 입력받아 정책과 매칭한다.

예상 프로필:

* 연령
* 거주지역
* 소득구간
* 취업상태
* 주거상태
* 금융 관심사

세부 항목은 실제 정책 데이터를 확인한 후 결정한다.

### ④ 쉬운 설명

복잡한 정책 원문을 사용자가 이해하기 쉬운 언어로 설명한다.

### ⑤ 관련성 설명

"왜 이 정책이 나에게 관련 있는지"를 설명한다.

### ⑥ 행동 단계

정책 활용을 위해 사용자가 해야 할 일을 단계별로 안내한다.

예:

1. 자격 조건 확인
2. 필요 서류 확인
3. 신청 방법 확인
4. 신청 기간 확인

### ⑦ 신규/변경 반영

새로운 정책 또는 기존 정책의 의미 있는 변경을 감지해 개인화 피드에 카드로 반영한다.
알림 발송은 하지 않는다 — 사용자가 피드에 접속했을 때 반영돼 있으면 된다.

---

## 5. 예상 데이터 흐름

### 수집

금융위 RSS
→ 신규 항목 감지
→ Google Sheets 저장

### AI 처리

정책 원문
→ Gemini
→ 구조화된 정책 데이터
→ Google Sheets 저장

### 개인화

사용자 프로필
+
정책 데이터
→ 관련성/조건 비교
→ 설명 생성
→ 행동 단계 생성

### 신규/변경 반영

신규/변경 정책
→ 사용자 관련성 확인
→ 개인화 피드에 카드 추가

---

## 6. AI 역할

### 사용 LLM

**Google Gemini 무료 모델**

### Gemini 주요 역할

* 정책 원문 정보 추출
* 정책 조건 구조화
* 쉬운 설명 생성
* 개인 관련성 설명
* 행동 단계 생성
* 정책 변경사항 설명

가능한 경우 조건 비교 등 명확한 로직은 일반 코드로 처리하고,
LLM은 비정형 문서 해석과 설명에 집중한다.

AI가 법적/행정적 자격을 최종 확정적으로 판단하지 않는다.

---

## 7. 기술 스택

* Backend: **Python + FastAPI**
* Database/Storage: **Google Sheets**
* Data Source: **금융위원회 RSS**
* LLM: **Google Gemini 무료 모델**
* Frontend: **프레임워크 없음 — 빌드 도구 없는 단일 HTML/CSS/JS** (2026-09-06 확정, 11절 참고)
* Deployment: **Vercel**
* User Identification: **Email** — 회원가입/로그인 없이 사용자를 구분하는 식별자 용도로만 쓴다.
  **알림 발송(Notification)에는 쓰지 않는다** — 알림 서비스는 만들지 않는다.

---

## 8. 데이터 구조

최종 스키마는 **확정**되었다 (2026-09-06). 상세 필드와 근거는 [docs/schema.md](docs/schema.md) 참고.

### 확정된 구조

```
articles_raw       1계층  원본        LLM 없음, 전 건 저장
articles_triage    2계층  분류        짧은 LLM 1회
articles_detail    3계층  상세        긴 LLM 1회, 일부만
```

3계층 진입 여부는 `doc_type`이 아니라 `personal_relevance`(개인이 지금 신청·이용·신고할 수 있는가)로 결정한다.

### 설계 원칙

실측 데이터에 등장하는 표현을 그대로 값으로 추가하다 보니 스키마가 계속 커지는 문제를 겪었다 (작업일지 002 참고). 이후 **처음부터 적은 수의 범용 값으로 커버되는지 먼저 확인하고, 부족한 부분만 늘리는** 방향으로 정리했다.

* `topics`(관심분야) 8개: 특정 자산군·사고성 이슈(가상자산/사기예방/신용·연체)는 `기타`로 묶어 **분류는 유지하되 개인화 매칭에서만 제외**한다.
* 사용자 프로필 입력 항목은 `region`/`gender`/`birth_date`/`occupation_type`/`interests` 5개로 고정한다.
* `occupation_type`(개인특성)과 정책의 `audience_groups`는 어휘를 통일한 5개 값(근로자/구직자/소상공인·자영업자/대학생/프리랜서·특수고용직)을 쓴다. 청년·고령층 같은 연령 기반 그룹은 `birth_date`로 코드에서 바로 계산하므로 별도 값을 두지 않는다.

값이 실제로 부족하다는 게 데이터로 확인되기 전에는 임의로 값을 추가하지 않는다.

---

## 9. 4주 범위 관리

우선순위:

1. RSS 수집 — 미착수
2. Google Sheets 적재 — **목업 데이터 20건으로 구조 검증 완료** (실시간 RSS 연동은 미착수, [작업일지 003](docs/worklogs/003-mock-database.md))
3. 정책 구조화 — 미착수 (목업 데이터는 수작업으로 미리 구조화해둠)
4. 개인화 — 미착수
5. 쉬운 설명 — 미착수
6. 행동 단계 — 미착수
7. 변경 감지 — 미착수
8. 최소 UI(카드 피드) — **첫 화면(사용자 프로필 입력 폼) 완료**, 카드 피드 화면은 미착수 ([작업일지 004](docs/worklogs/004-landing-page.md))

~~이메일 알림~~ — **제외됨** (2026-09-06). 알림 서비스는 만들지 않기로 했다. 이메일은 사용자
식별자로만 남는다 (7절 참고).

핵심 기능 완성 전에는 불필요한 기능을 추가하지 않는다.

---

## 10. 미정 사항

아래는 Claude가 임의로 결정하지 않는다.

* 정책 변경 감지 기준
* 구체적인 Gemini 모델

Google Sheets 세부 스키마, 사용자 프로필 세부 항목, 정책 JSON 최종 구조는 2026-09-06 확정되었다
([docs/schema.md](docs/schema.md) 참고, 8절 요약).
프론트엔드 프레임워크/UI 방향도 2026-09-06 확정되었다 (11절 참고).

필요한 경우 프로젝트 목표와 **4주라는 시간 제약**을 기준으로 사용자와 논의 후 결정한다.

---

## 11. 프론트엔드 구현 방침

빌드 도구, 프레임워크 없이 **`index.html` 단일 파일**로 화면을 만든다. 자세한 구현과 이유는
[작업일지 004](docs/worklogs/004-landing-page.md) 참고.

* 스타일: **Pico.css** (CDN). 클래스를 거의 안 붙여도 기본 태그가 정리돼 보이고, 반응형도 기본 제공된다.
* 데이터: 백엔드 API 없이, **Google Sheets를 CSV로 export한 주소를 PapaParse로 직접 읽는다.**
  이 방식을 쓰려면 시트가 "링크가 있는 모든 사용자 보기"로 공유돼 있어야 한다 —
  민감하지 않은 데이터에만 이 방식을 쓴다.
* 저장: 백엔드가 생기기 전까지 사용자 프로필은 `localStorage`에 저장한다.

이 방침은 정식 백엔드(FastAPI)가 생기기 전까지의 **임시 구조**다. 화면이 늘어나거나
백엔드 API가 생기면 이 절을 다시 논의한다.
